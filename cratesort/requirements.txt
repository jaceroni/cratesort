# GUI
PyQt6>=6.6.0

# ID3 tag reading/writing
mutagen>=1.47.0

# Audio fingerprinting (duplicate detection)
pyacoustid>=1.3.0

# Serato crate reading/writing
serato-crate>=0.1.0

# YouTube downloader (used by the YouTube Import feature). YouTube changes
# its protections often enough that yt-dlp ships near-weekly fixes — an
# install more than ~1-2 months old routinely 403s on downloads even though
# metadata fetches still succeed. Keep this current, not just above-floor.
yt-dlp>=2026.8.19

# Bundled ffmpeg binary (used by YouTube Import and the WAV/MOV converters)
imageio-ffmpeg>=0.5.0

# Optional: higher-level Serato metadata API (reference/fallback)
# serato-tools>=0.1.0
